{
  "name": "vue2-widget-lib",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vue-cli-service build",
    "build:all": "rm -rf dist && npm run build:sales && npm run build:chart && npm run build:filter && npm run build:orders",
    "build:sales": "cross-env WIDGET_NAME=bi-sales-panel vue-cli-service build --no-clean",
    "build:chart": "cross-env WIDGET_NAME=bi-chart-panel vue-cli-service build --no-clean",
    "build:filter": "cross-env WIDGET_NAME=bi-filter-bar vue-cli-service build --no-clean",
    "build:orders": "cross-env WIDGET_NAME=bi-orders-panel vue-cli-service build --no-clean",
    "serve": "vue-cli-service serve",
    "serve:widget": "cross-env WIDGET_BUILD=true vue-cli-service build --watch --mode development",
    "serve:dist": "npx serve dist -l 8081 --cors"
  },
  "dependencies": {
    "element-ui": "^2.15.14",
    "vue": "^2.6.14"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "~5.0.8",
    "@vue/cli-service": "~5.0.8",
    "@vue/web-component-wrapper": "^1.3.0",
    "@wc/widget-wrapper-plugin": "workspace:*",
    "vue-template-compiler": "^2.6.14"
  }
}
