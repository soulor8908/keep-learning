{
  "dependencies": {
    "netlify-cli": "^26.1.0",
    "puppeteer-core": "^25.2.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "happy-dom": "^14.0.0"
  },
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:ci": "vitest run --reporter=verbose"
  }
}
